# components

Shared, view-agnostic building blocks: the UI primitives (Radix + cva, styled from semantic tokens) and the frosted-glass surface that floating panels compose. Add primitives with `npx shadcn add <name>`.
