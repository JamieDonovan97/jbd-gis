# features/gis

A full-screen map with floating control surfaces layered over it — a tool dock, an inspector panel, map navigation, and view toggles. Map and UI state live in the feature's store; server data comes through TanStack Query; tools, layers, and basemaps are registries.
