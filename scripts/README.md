# scripts

Developer utilities, run from the repo root.

| Script | Purpose |
|---|---|
| `dev.sh` | Local workflow — `setup` (enable git hooks, once per clone), `db`, `api`, `web`, `up`, `down`. |
| `update-structure.sh` | Regenerate `docs/structure.md` from the file tree. |
